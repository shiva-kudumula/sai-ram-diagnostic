import {useEffect,useState} from 'react';import api from '../services/api';
export default function BookingHistory(){const [bookings,setBookings]=useState([]),[error,setError]=useState('');useEffect(()=>{api.get('/bookings/my-bookings').then(r=>setBookings(r.data.bookings)).catch(e=>setError(e.response?.data?.message||'Could not load bookings.'));},[]);return <div className="container py-5"><h1>Booking History</h1>{error&&<div className="alert alert-danger">{error}</div>}<div className="table-responsive"><table className="table align-middle"><thead><tr><th>Test</th><th>Date</th><th>Price</th><th>Status</th><th>Booked</th></tr></thead><tbody>{bookings.map(b=><tr key={b._id}><td>{b.testName}</td><td>{new Date(b.testDate).toLocaleDateString()}</td><td>₹{b.price}</td><td><span className={`status ${b.status.toLowerCase()}`}>{b.status}</span></td><td>{new Date(b.createdAt).toLocaleDateString()}</td></tr>)}</tbody></table></div>{!bookings.length&&!error&&<p className="text-muted">You have no bookings yet.</p>}</div>}
